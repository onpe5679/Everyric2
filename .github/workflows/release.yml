name: Release

on:
  push:
    tags:
      - "engine-v*"
      - "chrome-v*"

permissions:
  contents: write

jobs:
  engine:
    if: startsWith(github.ref_name, 'engine-v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Verify tag matches pyproject version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#engine-v}"
          PYPROJECT_VERSION="$(grep -m1 '^version' pyproject.toml | sed 's/.*"\(.*\)".*/\1/')"
          if [ "$TAG_VERSION" != "$PYPROJECT_VERSION" ]; then
            echo "tag version ($TAG_VERSION) != pyproject version ($PYPROJECT_VERSION)"
            exit 1
          fi
      - run: uv build
      - name: Pick release notes
        id: notes
        run: |
          if [ -f "docs/releases/${GITHUB_REF_NAME}.md" ]; then
            echo "path=docs/releases/${GITHUB_REF_NAME}.md" >> "$GITHUB_OUTPUT"
          fi
      - uses: softprops/action-gh-release@v2
        with:
          body_path: ${{ steps.notes.outputs.path }}
          generate_release_notes: true
          files: |
            dist/*.whl
            dist/*.tar.gz

  chrome:
    if: startsWith(github.ref_name, 'chrome-v')
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: everyric2-chrome
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: everyric2-chrome/package-lock.json
      - name: Verify tag matches package/manifest version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#chrome-v}"
          PKG_VERSION="$(node -p "require('./package.json').version")"
          MF_VERSION="$(node -p "require('./manifest.json').version")"
          if [ "$TAG_VERSION" != "$PKG_VERSION" ] || [ "$TAG_VERSION" != "$MF_VERSION" ]; then
            echo "tag ($TAG_VERSION) != package.json ($PKG_VERSION) / manifest.json ($MF_VERSION)"
            exit 1
          fi
      - run: npm ci
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
      - run: npm run build
      - name: Zip extension
        run: |
          cd dist
          zip -r "../Everyric-Chrome-${GITHUB_REF_NAME#chrome-v}.zip" .
      - name: Pick release notes
        id: notes
        working-directory: .
        run: |
          if [ -f "docs/releases/${GITHUB_REF_NAME}.md" ]; then
            echo "path=docs/releases/${GITHUB_REF_NAME}.md" >> "$GITHUB_OUTPUT"
          fi
      - uses: softprops/action-gh-release@v2
        with:
          body_path: ${{ steps.notes.outputs.path }}
          generate_release_notes: true
          files: everyric2-chrome/Everyric-Chrome-*.zip

  update-latest:
    needs: [engine, chrome]
    if: always() && (needs.engine.result == 'success' || needs.chrome.result == 'success')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
      - run: node .github/scripts/update-latest.mjs "$GITHUB_REF_NAME"
      - name: Commit latest.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add latest.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "chore: update latest.json for ${GITHUB_REF_NAME}"
          git push origin master
